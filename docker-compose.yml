services:
  backend:
    build: .
    container_name: tattoo44_backend
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./db:/app/db
      - ./system_prompt.txt:/app/system_prompt.txt:ro
    restart: unless-stopped
